class Start {
    constructor(ctx, canvas) {
        this.ctx = ctx;
        this.canvas = canvas;
    }

    drawTitle() {
        this.ctx.beginPath();
        this.ctx.moveTo()
    }
}

export default Start;